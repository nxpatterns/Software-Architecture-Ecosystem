# WireGuard

## Installation

```bash
# Check OS + version (Linux)
cat /etc/os-release

# macOS (Homebrew)
brew install wireguard-tools
# Linux (Debian/Ubuntu)
sudo apt install wireguard
# Windows (Chocolatey)
choco install wireguard
```
